part of '../locations_screen.dart';

class LocationsBody extends StatelessWidget {
  const LocationsBody({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: CustomText.ourText("Locations Screen")),
    );
  }
}
